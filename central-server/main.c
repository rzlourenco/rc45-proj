#include <netdb.h>
#include <netinet/in.h>
#include <stdlib.h>
#include <stdio.h>
#include <sys/socket.h>
#include <unistd.h>



int main(int argc, char *argv[]) {
  // set up udp
  // set up tcp

  // bind
  // listen

  for (;;) {
    // newfd = accept
    // fork
    // if parent
    //   close newfd
    //   continue
    // handle newfd
  }

  return 0;
}

